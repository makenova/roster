$(function () {
  $('#year-list').on('change', function () {
    var semesterList = $('#semester-list');
    semesterList.empty();
    semesterList.parent().removeClass('disabled hidden');
    semesterlist.appens
    console.log($(this).val());
  })
});